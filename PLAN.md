# V1 → V2

V1 核心：把「文件 → 時間碼 → Clipboard → 快捷鍵 → 進度」固定成單一控制面板。

V1 驗收：DOCX 在 Win/Mac 都能解析；來源 IN/OUT/整段複製正確；Loop 名稱可直接複製整串時間碼；特殊類型可分類；完成狀態可保存；快捷鍵按鈕會回到 Nuendo 執行。

V1.1：專案 JSON 匯入/匯出、Always On Top、自訂全域快捷鍵、錯誤行人工修正。

V2：Locator Automation + Macro Runner。建議拆成 Prepare Source 與 Move/Copy 兩段，中間保留人工確認；SLOW/FREEZE/NEW SHOT 永遠顯示警告，不直接全自動結案。
